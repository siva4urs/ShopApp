import React from 'react'
import { Text, View, SafeAreaView } from 'react-native'

export default function App() {
  return (
    <SafeAreaView style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>ShopApp Mobile (Expo)</Text>
    </SafeAreaView>
  )
}
